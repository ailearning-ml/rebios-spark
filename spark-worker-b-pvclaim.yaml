apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spark-worker-b-volume-claim-0
  labels:
    app: spark-worker-b
spec:
  storageClassName: manual
  volumeName: spark-worker-b-volume-0
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10M
---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spark-worker-b-volume-claim-1
  labels:
    app: spark-worker-b
spec:
  storageClassName: manual
  volumeName: spark-worker-b-volume-1  
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10M
---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spark-worker-b-volume-claim-2
  labels:
    app: spark-worker-b
spec:
  storageClassName: manual
  volumeName: spark-worker-b-volume-2
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10M

---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spark-worker-b-volume-claim-3
  labels:
    app: spark-worker-b
spec:
  storageClassName: manual
  volumeName: spark-worker-b-volume-3
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10M
---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spark-worker-b-volume-claim-4
  labels:
    app: spark-worker-b
spec:
  storageClassName: manual
  volumeName: spark-worker-b-volume-4
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10M
